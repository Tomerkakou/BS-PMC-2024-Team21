{
    "name": "learnix-app",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm run start --prefix ./fe\" \"npm run start-flask\"",
        "start-react": "react-scripts start",
        "start-flask": "cd ./be && flask run",
        "init-dev": "npm i && cd ./fe && npm i && cd ../be && pip install -r requirements.txt"
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}
